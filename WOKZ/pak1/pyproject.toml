[project]
name = "pak1"
version = "0.1.0"
description = "CLI Application for testing"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=13.9.2",
]

[project.scripts]
pak1 = "pak1:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
